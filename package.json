{
  "name": "booster-doc",
  "version": "1.0.0",
  "description": "booster documents",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "npx gitbook serve ./ ./docs",
    "build": "npx gitbook build ./ ./docs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gitbook-cli": "^2.3.2",
    "live-server": "^1.2.1"
  }
}
